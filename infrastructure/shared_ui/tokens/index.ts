/**
 * CAPVIA Shared UI Design System - Design Tokens Index
 */

import { colors } from './colors';
import { typography } from './typography';
import { radius } from './radius';
import { shadows } from './shadows';
import { spacing } from './spacing';

export const tokens = {
  colors,
  typography,
  radius,
  shadows,
  spacing,
};

export { colors, typography, radius, shadows, spacing };
